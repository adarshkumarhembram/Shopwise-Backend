# 🛒 ShopWise Backend (Node + Express + MongoDB)

A professional backend for an e-commerce app using Node.js, Express, and MongoDB.

## 🚀 Setup Instructions

1. Clone the repository
2. Install dependencies  
   ```bash
   npm install
